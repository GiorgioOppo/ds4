# GLM 5.2 configuration tests

These metadata-only GGUF fixtures verify the exact GLM 5.2 shape without
requiring the full model.  Tests cover derived geometry, architecture isolation,
shape mismatches, F64-compatible float metadata and expert normalization.
