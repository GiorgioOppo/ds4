# GLM 5.2 tokenizer tests

These model-free tests pin the `glm4` split boundaries, Unicode number and
punctuation handling, GPT-2 byte round trips, atomic rendered-chat controls,
reasoning-aware stop tokens, and prompt-injection containment.
