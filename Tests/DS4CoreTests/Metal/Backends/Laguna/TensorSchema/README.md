**English** | [Italiano](README.it.md)

# Laguna tensor-schema tests

Record-based coverage of `LagunaTensorSchema` without mapping a 60+ GB
fixture: both published recipes (Q8_0 signal path and legacy Q4_K/F16), the
mixed Q2_K/Q3_K routed file, per-layer routed-type coherence, the layout
marker rule, per-layer 48/72 query widths, layer-only views identified from
attention Q, and the missing/partial/duplicate error taxonomy.
