# Kimi K3 Metal backend

Native backend scaffolding for `general.architecture = kimi-k3`.

Geometry, tensor schema, reference, kernels, streaming and engine are kept
separate as in the GLM and Laguna backends. No implementation may enable the
gate before real-weight parity.
