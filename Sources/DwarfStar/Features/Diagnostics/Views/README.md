**English** | [Italiano](README.it.md)

# Diagnostics Views

`DiagnosticsView.swift` presents token/template inspection and includes
`EngineConsole`, which displays the captured engine-log tail.

This directory is presentation-only. Add diagnostic computation to the
controller and reusable log capture to `Shared/Support`.

