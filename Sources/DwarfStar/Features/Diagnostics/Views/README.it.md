# Viste di diagnostica

`DiagnosticsView.swift` presenta l'ispezione di token/template e include
`EngineConsole`, che mostra la coda catturata del log del motore.

Questa directory è di sola presentazione. Aggiungi il calcolo diagnostico al
controller e la cattura riutilizzabile dei log a `Shared/Support`.
