# DwarfStar/Project

- **`ProjectView.swift`** — libreria progetti: importa cartelle (bookmark sandbox), le indicizza nella `ProjectCache`, e seleziona il progetto attivo su cui operano i tool `project_*`/`file_*` degli agenti. L'import non tocca la memoria della chat.
