# DwarfStar/Tuning

- **`TuningView.swift`** — pannello Tuning: slot della cache esperti, hit-rate, concentrazione del routing per layer (la "usage imatrix").
- **`AgentsView.swift`** — editor degli agenti (ruoli): prompt, icona, tool per agente, export/import JSON. *(NB: riguarda gli agenti più che il tuning — candidato a una cartella `Agents/` dedicata.)*
